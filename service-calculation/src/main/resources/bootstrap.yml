server:
  port: 3000

spring:
  application:
    name: service-calculation

spring:
  cloud:
    config:
      label: master
      profile: test
      discovery:
        enabled: true
        serviceId: server-config

eureka:
  client:
    serviceUrl:
      defaultZone: http://aliyun.roothk.top:1000/eureka/

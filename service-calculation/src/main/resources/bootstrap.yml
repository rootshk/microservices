server:
  port: 3000

eureka:
  client:
    serviceUrl:
      defaultZone: http://aliyun.roothk.top:1000/eureka/

spring:
  application:
    name: service-calculation
  cloud:
    config:
      label: master
      profile: test
      discovery:
        enabled: true
        serviceId: server-config


